pre-commit:
    parallel: true
    commands:
        prettier:
            glob: 'src/**'
            run: npm run format
        eslint:
            glob: 'src/**/*.{ts, tsx}'
            run: npm run lint
